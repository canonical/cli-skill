---
description: "Analyze a CLI command: review UX, flags, output, errors, and docs. Produces a full analysis in 0-analysis/."
---

Use the `cli-review` skill. Run the `analyze-cli` workflow.

The user wants to analyze a CLI command. Follow the "Primary Command: analyze-cli" section of the skill exactly.

Collect required inputs (command name, syntax, flags, outputs, exit codes, target users, shell constraints). If context is missing, ask focused follow-up questions.

Generate the `0-analysis/` directory with all required files in the specified build order.
